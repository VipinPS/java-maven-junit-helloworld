dist: xenial
language: java
jdk:
- openjdk11
before_install:
- git fetch --all --tags
- export version=`git describe --abbrev=0 --tags`
- echo $version
deploy:
  provider: script
  script: bash echo $version
  on:
    tags: true
    all_branches: true 
